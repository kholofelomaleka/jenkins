Jenkins application for day to day use.
- Fully dockerized containers:
├── docker-compose.yml
├── jenkins-master/
│   ├── Dockerfile
│   ├── plugins.txt
│   └── jenkins.yaml 
├── jenkins-nginx/
│   ├── Dockerfile
│   └── jenkins.conf
└── jenkins-slave/
    └── Dockerfile 

